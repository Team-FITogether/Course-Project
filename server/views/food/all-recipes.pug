extends ../sub-headers/sub-header-default
block content
    link(rel="stylesheet" href="/css/articles/all-articles.css")
    each val in recipes
        div.article-container
            a(href="/recipes/single-recipe?title=" + val.title) #{val.title}
            div.divider